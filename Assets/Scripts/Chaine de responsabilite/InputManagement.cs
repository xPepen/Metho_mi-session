using System.Collections.Generic;

public class InputManagement : MainBehaviour
{
    /*private List<IInputHandler> m_ListOfInputHandler;
    protected override void OnAwake()
    {
        base.OnAwake();
        m_ListOfInputHandler = new List<IInputHandler>();
    }

    private void InputHandle()
    {
        for (int i = m_ListOfInputHandler.Count; i > 0; i--)
        {
            if (m_ListOfInputHandler[i].HandleInput())
            {
                break;
            }
        }
    }

    private void AddHandler(IInputHandler refInterface)
    {
        m_ListOfInputHandler.Add(refInterface);
    }
    private void RemoveHandler(IInputHandler refInterface)
    {
        m_ListOfInputHandler.Remove(refInterface);
    }*/
   
    
}
